# Идеи и бэклог (актуальное)

Только то, что еще не реализовано. Выполненные задачи из этого файла удаляются.

---

## Клиентский вход из канала в поиск

Канал остается лентой объявлений. Полноценный интерактивный фильтр внутри самой ленты сделать нельзя, поэтому точка входа в поиск остается через текущий сценарий бота в личке.

### Принятое решение

- Mini App исключен из плана.
- Развиваем текущую реализацию поиска в одном боте.
- Точка входа: ссылка из канала на бота с параметром `start` (`filter`, `client`, `search`).
- Поиск остается на текущем стеке Python + SQLite.

---

## Личный кабинет и админка: что еще доделать

### Личный кабинет (опционально)

- Счетчик публикаций за месяц.
- Черновики объявлений.

### Админка (заглушки, которые нужно реализовать)

- `adm_stats` — экран статистики.
- `export_stats` — экспорт статистики.
- `adm_logs` — просмотр логов.
- `export_logs` — экспорт логов.
- `adm_search_posts` — поиск публикаций.
